# llm-rubric-judge
Python framework executing deterministic code checks alongside LLM-as-a-judge scoring with confidence thresholds. Combines hard deterministic gates with LLM-judge scoring, where low-confidence judgments get flagged rather than trusted blindly.
